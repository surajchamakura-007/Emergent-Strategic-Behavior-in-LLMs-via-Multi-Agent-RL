# Stage 1, run 1 of 4 — Dr. GRPO at T=5, seed 1.
# Authority: PRD v6.1 §2.3 + STAGE1_EXECUTION_PLAN_v3.1 §4.
# Only T and seed differ across the 4 configs; everything else is locked
# in `configs/config.py` defaults.

run_name: drgrpo_T5_seed1_stage1
seed: 1

# Payoff matrix — IPD with T=5 (PRD §3 anchor case).
payoffs:
  T: 5.0
  R: 3.0
  P: 1.0
  S: 0.0

# W&B tags get T- and seed-specific labels appended for filtering.
wandb_tags:
  - stage1
  - drgrpo
  - prd_v6_1
  - T5
  - seed1
